# Tzeri's Garden

### How to Run (MacOS/Linux/Windows)
1. Ensure Python 3.10+ is installed.
2. Install dependencies:
   `pip install -r requirements.txt`
3. Run the game:
   `python main.py` 

*Note: A Windows-compiled version is available in the /dist folder.*